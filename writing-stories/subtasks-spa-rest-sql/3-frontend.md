We will need to:


- Specify what reusable UI components are needed. As we have access to the codebase, we should look and see if they are already available (and look for component libraries in package.json).
  - if changes to resuable UI components are needed or new components are needed, we should create additional subtasks to make these changes

- Specify changes in existing components that incorporate the changes.
- If necessary, specify changes in routing to support new pages.
- Specify any changes in the frontend's data model.
