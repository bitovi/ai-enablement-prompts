There should should be API guidelines in the instructions files (or referenced from the instructions files). If there aren't, stop. They need to be created.


Given the existing API, we will need to design the API changes.

Write out the changes in `{story-slug}-api-changes.md`.

Include in the subtask any implementation details (like use of a DataModel). Note any authorization considerations.

Write out the data model after the change in complete form in `{story-slug}-openapi.json`
